# Smart Rockets

A Python learning project inspired by Daniel Shiffman's *Nature of Code* Smart Rockets (genetic algorithms).

## Setup

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

Requires Python 3.11+ (3.14 works with `pygame-ce`).
